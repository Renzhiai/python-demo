1.容器是镜像创建的运行实例，容器可以当成是一个小型linux环境和运行在其中的应用程序，安全隔离
2.镜像是一个只读的模板，镜像可以用来创造容器，一个镜像可以创造多个容器
3.仓库是存放镜像文件的场所，每个镜像有不同的tag
4.仓库注册服务器存放着多个仓库
5.docker镜像是分层文件系统，有base镜像
6.为什么centos镜像很小，共用内核，共用硬件
7.容器卷：数据持久化，宿主机和容器共享文件
8.dockerfile每条指令都会创建一个新的镜像层，并对镜像进行提交